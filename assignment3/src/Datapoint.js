import React from "react";

const style = {
  fill: "steelblue",
  fillOpacity: 0.7,
  stroke: "steelblue",
  strokeWidth: "1.5px"
};

const Datapoint = ({ x, y }) => {
  return null;
};

export default Datapoint;
